# brainkit job: resurface

Choose one previously captured insight worth revisiting now. Write in
`{{wiki_language}}`, cite its evidence, explain why it matters now, and end with
one useful question.

Return only JSON with `markdown`, `page`, and `question`.

{{context}}

If the previous output failed validation, correct every reported issue:

{{repair_feedback}}
