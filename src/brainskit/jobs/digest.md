# brainskit job: digest

Create a compact operational digest in `{{wiki_language}}` for the period
`{{since}}`. Optimize for action and habit formation. Include: new captures,
pending ingestion, filings that need review, health issues, one resurfacing
prompt, and the smallest useful next action. Do not invent activity.

Return only JSON with `markdown`, `actions`, and `resurfaced`.

Vault status:

{{status}}

Recent source registry records:

{{sources}}

Pending and recent filing proposals:

{{proposals}}

Freshness state:

{{freshness}}

If the previous output failed validation, correct every reported issue:

{{repair_feedback}}
